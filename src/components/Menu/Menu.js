import React, { Component } from 'react';
import styles from './Menu.module.sass'

function Menu() {
    return (
    <div className={styles.Menu}>
       <h1>LOGO</h1>
    </div>
    );
}

export default Menu;